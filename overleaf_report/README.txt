Overleaf package contents:
- main.tex
- figures/*.png

How to use:
1) Upload the whole `overleaf_report/` folder to Overleaf.
2) Set `main.tex` as the main file (if needed).
3) Compile with pdfLaTeX.
